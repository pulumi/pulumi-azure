// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.azure.netapp.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.exceptions.MissingRequiredPropertyException;
import java.lang.String;
import java.util.Objects;


public final class VolumeBucketKeyVaultArgs extends com.pulumi.resources.ResourceArgs {

    public static final VolumeBucketKeyVaultArgs Empty = new VolumeBucketKeyVaultArgs();

    /**
     * The URI of the Azure Key Vault that stores the bucket server certificate.
     * 
     */
    @Import(name="certificateKeyVaultUri", required=true)
    private Output<String> certificateKeyVaultUri;

    /**
     * @return The URI of the Azure Key Vault that stores the bucket server certificate.
     * 
     */
    public Output<String> certificateKeyVaultUri() {
        return this.certificateKeyVaultUri;
    }

    /**
     * The name of the certificate object inside the Key Vault.
     * 
     */
    @Import(name="certificateName", required=true)
    private Output<String> certificateName;

    /**
     * @return The name of the certificate object inside the Key Vault.
     * 
     */
    public Output<String> certificateName() {
        return this.certificateName;
    }

    /**
     * The URI of the Azure Key Vault used to store the generated bucket access and secret keys. May be the same vault as `certificateKeyVaultUri` but the documentation recommends using two separate vaults.
     * 
     */
    @Import(name="credentialsKeyVaultUri", required=true)
    private Output<String> credentialsKeyVaultUri;

    /**
     * @return The URI of the Azure Key Vault used to store the generated bucket access and secret keys. May be the same vault as `certificateKeyVaultUri` but the documentation recommends using two separate vaults.
     * 
     */
    public Output<String> credentialsKeyVaultUri() {
        return this.credentialsKeyVaultUri;
    }

    /**
     * The name of the secret in `credentialsKeyVaultUri` that stores the generated bucket credentials. The Key Vault secret value is a JSON document with `accessKeyId` and `secretAccessKey` properties.
     * 
     * &gt; **Note:** When `keyVault` is used, the parent NetApp account must have a system-assigned managed identity (`identity { type = &#34;SystemAssigned&#34; }` on `azure.netapp.Account`). That identity is the principal that needs Key Vault access. Grant it `Get, List, Update, Create, Import, ManageContacts, GetIssuers, ListIssuers, SetIssuers, DeleteIssuers` certificate permissions on `certificateKeyVaultUri` and `Get, List, Set, Delete` secret permissions on `credentialsKeyVaultUri`.
     * 
     */
    @Import(name="credentialsSecretName", required=true)
    private Output<String> credentialsSecretName;

    /**
     * @return The name of the secret in `credentialsKeyVaultUri` that stores the generated bucket credentials. The Key Vault secret value is a JSON document with `accessKeyId` and `secretAccessKey` properties.
     * 
     * &gt; **Note:** When `keyVault` is used, the parent NetApp account must have a system-assigned managed identity (`identity { type = &#34;SystemAssigned&#34; }` on `azure.netapp.Account`). That identity is the principal that needs Key Vault access. Grant it `Get, List, Update, Create, Import, ManageContacts, GetIssuers, ListIssuers, SetIssuers, DeleteIssuers` certificate permissions on `certificateKeyVaultUri` and `Get, List, Set, Delete` secret permissions on `credentialsKeyVaultUri`.
     * 
     */
    public Output<String> credentialsSecretName() {
        return this.credentialsSecretName;
    }

    private VolumeBucketKeyVaultArgs() {}

    private VolumeBucketKeyVaultArgs(VolumeBucketKeyVaultArgs $) {
        this.certificateKeyVaultUri = $.certificateKeyVaultUri;
        this.certificateName = $.certificateName;
        this.credentialsKeyVaultUri = $.credentialsKeyVaultUri;
        this.credentialsSecretName = $.credentialsSecretName;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(VolumeBucketKeyVaultArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private VolumeBucketKeyVaultArgs $;

        public Builder() {
            $ = new VolumeBucketKeyVaultArgs();
        }

        public Builder(VolumeBucketKeyVaultArgs defaults) {
            $ = new VolumeBucketKeyVaultArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param certificateKeyVaultUri The URI of the Azure Key Vault that stores the bucket server certificate.
         * 
         * @return builder
         * 
         */
        public Builder certificateKeyVaultUri(Output<String> certificateKeyVaultUri) {
            $.certificateKeyVaultUri = certificateKeyVaultUri;
            return this;
        }

        /**
         * @param certificateKeyVaultUri The URI of the Azure Key Vault that stores the bucket server certificate.
         * 
         * @return builder
         * 
         */
        public Builder certificateKeyVaultUri(String certificateKeyVaultUri) {
            return certificateKeyVaultUri(Output.of(certificateKeyVaultUri));
        }

        /**
         * @param certificateName The name of the certificate object inside the Key Vault.
         * 
         * @return builder
         * 
         */
        public Builder certificateName(Output<String> certificateName) {
            $.certificateName = certificateName;
            return this;
        }

        /**
         * @param certificateName The name of the certificate object inside the Key Vault.
         * 
         * @return builder
         * 
         */
        public Builder certificateName(String certificateName) {
            return certificateName(Output.of(certificateName));
        }

        /**
         * @param credentialsKeyVaultUri The URI of the Azure Key Vault used to store the generated bucket access and secret keys. May be the same vault as `certificateKeyVaultUri` but the documentation recommends using two separate vaults.
         * 
         * @return builder
         * 
         */
        public Builder credentialsKeyVaultUri(Output<String> credentialsKeyVaultUri) {
            $.credentialsKeyVaultUri = credentialsKeyVaultUri;
            return this;
        }

        /**
         * @param credentialsKeyVaultUri The URI of the Azure Key Vault used to store the generated bucket access and secret keys. May be the same vault as `certificateKeyVaultUri` but the documentation recommends using two separate vaults.
         * 
         * @return builder
         * 
         */
        public Builder credentialsKeyVaultUri(String credentialsKeyVaultUri) {
            return credentialsKeyVaultUri(Output.of(credentialsKeyVaultUri));
        }

        /**
         * @param credentialsSecretName The name of the secret in `credentialsKeyVaultUri` that stores the generated bucket credentials. The Key Vault secret value is a JSON document with `accessKeyId` and `secretAccessKey` properties.
         * 
         * &gt; **Note:** When `keyVault` is used, the parent NetApp account must have a system-assigned managed identity (`identity { type = &#34;SystemAssigned&#34; }` on `azure.netapp.Account`). That identity is the principal that needs Key Vault access. Grant it `Get, List, Update, Create, Import, ManageContacts, GetIssuers, ListIssuers, SetIssuers, DeleteIssuers` certificate permissions on `certificateKeyVaultUri` and `Get, List, Set, Delete` secret permissions on `credentialsKeyVaultUri`.
         * 
         * @return builder
         * 
         */
        public Builder credentialsSecretName(Output<String> credentialsSecretName) {
            $.credentialsSecretName = credentialsSecretName;
            return this;
        }

        /**
         * @param credentialsSecretName The name of the secret in `credentialsKeyVaultUri` that stores the generated bucket credentials. The Key Vault secret value is a JSON document with `accessKeyId` and `secretAccessKey` properties.
         * 
         * &gt; **Note:** When `keyVault` is used, the parent NetApp account must have a system-assigned managed identity (`identity { type = &#34;SystemAssigned&#34; }` on `azure.netapp.Account`). That identity is the principal that needs Key Vault access. Grant it `Get, List, Update, Create, Import, ManageContacts, GetIssuers, ListIssuers, SetIssuers, DeleteIssuers` certificate permissions on `certificateKeyVaultUri` and `Get, List, Set, Delete` secret permissions on `credentialsKeyVaultUri`.
         * 
         * @return builder
         * 
         */
        public Builder credentialsSecretName(String credentialsSecretName) {
            return credentialsSecretName(Output.of(credentialsSecretName));
        }

        public VolumeBucketKeyVaultArgs build() {
            if ($.certificateKeyVaultUri == null) {
                throw new MissingRequiredPropertyException("VolumeBucketKeyVaultArgs", "certificateKeyVaultUri");
            }
            if ($.certificateName == null) {
                throw new MissingRequiredPropertyException("VolumeBucketKeyVaultArgs", "certificateName");
            }
            if ($.credentialsKeyVaultUri == null) {
                throw new MissingRequiredPropertyException("VolumeBucketKeyVaultArgs", "credentialsKeyVaultUri");
            }
            if ($.credentialsSecretName == null) {
                throw new MissingRequiredPropertyException("VolumeBucketKeyVaultArgs", "credentialsSecretName");
            }
            return $;
        }
    }

}
