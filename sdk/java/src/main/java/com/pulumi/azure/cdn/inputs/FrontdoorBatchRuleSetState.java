// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.azure.cdn.inputs;

import com.pulumi.azure.cdn.inputs.FrontdoorBatchRuleSetRuleArgs;
import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import java.lang.String;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class FrontdoorBatchRuleSetState extends com.pulumi.resources.ResourceArgs {

    public static final FrontdoorBatchRuleSetState Empty = new FrontdoorBatchRuleSetState();

    /**
     * The resource ID of the Front Door Profile where this Front Door Batch Rule Set should be created. Changing this forces a new resource to be created.
     * 
     */
    @Import(name="cdnFrontdoorProfileId")
    private @Nullable Output<String> cdnFrontdoorProfileId;

    /**
     * @return The resource ID of the Front Door Profile where this Front Door Batch Rule Set should be created. Changing this forces a new resource to be created.
     * 
     */
    public Optional<Output<String>> cdnFrontdoorProfileId() {
        return Optional.ofNullable(this.cdnFrontdoorProfileId);
    }

    /**
     * The name which should be used for this Front Door Batch Rule Set. Changing this forces a new resource to be created.
     * 
     */
    @Import(name="name")
    private @Nullable Output<String> name;

    /**
     * @return The name which should be used for this Front Door Batch Rule Set. Changing this forces a new resource to be created.
     * 
     */
    public Optional<Output<String>> name() {
        return Optional.ofNullable(this.name);
    }

    /**
     * One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block&#39;s `order` value. A maximum of `100` `rule` blocks may be defined.
     * 
     * &gt; **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
     * 
     * &gt; **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
     * 
     * &gt; **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
     * 
     */
    @Import(name="rules")
    private @Nullable Output<List<FrontdoorBatchRuleSetRuleArgs>> rules;

    /**
     * @return One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block&#39;s `order` value. A maximum of `100` `rule` blocks may be defined.
     * 
     * &gt; **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
     * 
     * &gt; **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
     * 
     * &gt; **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
     * 
     */
    public Optional<Output<List<FrontdoorBatchRuleSetRuleArgs>>> rules() {
        return Optional.ofNullable(this.rules);
    }

    private FrontdoorBatchRuleSetState() {}

    private FrontdoorBatchRuleSetState(FrontdoorBatchRuleSetState $) {
        this.cdnFrontdoorProfileId = $.cdnFrontdoorProfileId;
        this.name = $.name;
        this.rules = $.rules;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(FrontdoorBatchRuleSetState defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private FrontdoorBatchRuleSetState $;

        public Builder() {
            $ = new FrontdoorBatchRuleSetState();
        }

        public Builder(FrontdoorBatchRuleSetState defaults) {
            $ = new FrontdoorBatchRuleSetState(Objects.requireNonNull(defaults));
        }

        /**
         * @param cdnFrontdoorProfileId The resource ID of the Front Door Profile where this Front Door Batch Rule Set should be created. Changing this forces a new resource to be created.
         * 
         * @return builder
         * 
         */
        public Builder cdnFrontdoorProfileId(@Nullable Output<String> cdnFrontdoorProfileId) {
            $.cdnFrontdoorProfileId = cdnFrontdoorProfileId;
            return this;
        }

        /**
         * @param cdnFrontdoorProfileId The resource ID of the Front Door Profile where this Front Door Batch Rule Set should be created. Changing this forces a new resource to be created.
         * 
         * @return builder
         * 
         */
        public Builder cdnFrontdoorProfileId(String cdnFrontdoorProfileId) {
            return cdnFrontdoorProfileId(Output.of(cdnFrontdoorProfileId));
        }

        /**
         * @param name The name which should be used for this Front Door Batch Rule Set. Changing this forces a new resource to be created.
         * 
         * @return builder
         * 
         */
        public Builder name(@Nullable Output<String> name) {
            $.name = name;
            return this;
        }

        /**
         * @param name The name which should be used for this Front Door Batch Rule Set. Changing this forces a new resource to be created.
         * 
         * @return builder
         * 
         */
        public Builder name(String name) {
            return name(Output.of(name));
        }

        /**
         * @param rules One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block&#39;s `order` value. A maximum of `100` `rule` blocks may be defined.
         * 
         * &gt; **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
         * 
         * &gt; **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
         * 
         * &gt; **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
         * 
         * @return builder
         * 
         */
        public Builder rules(@Nullable Output<List<FrontdoorBatchRuleSetRuleArgs>> rules) {
            $.rules = rules;
            return this;
        }

        /**
         * @param rules One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block&#39;s `order` value. A maximum of `100` `rule` blocks may be defined.
         * 
         * &gt; **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
         * 
         * &gt; **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
         * 
         * &gt; **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
         * 
         * @return builder
         * 
         */
        public Builder rules(List<FrontdoorBatchRuleSetRuleArgs> rules) {
            return rules(Output.of(rules));
        }

        /**
         * @param rules One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block&#39;s `order` value. A maximum of `100` `rule` blocks may be defined.
         * 
         * &gt; **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
         * 
         * &gt; **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
         * 
         * &gt; **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
         * 
         * @return builder
         * 
         */
        public Builder rules(FrontdoorBatchRuleSetRuleArgs... rules) {
            return rules(List.of(rules));
        }

        public FrontdoorBatchRuleSetState build() {
            return $;
        }
    }

}
