// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azure from "@pulumi/azure";
 *
 * const example = new azure.appservice.SourceControlToken("example", {
 *     type: "GitHub",
 *     token: "ghp_sometokenvaluesecretsauce",
 * });
 * ```
 *
 * ## API Providers
 *
 * <!-- This section is generated, changes will be overwritten -->
 * This resource uses the following Azure API Providers:
 *
 * * `Microsoft.Web` - 2023-01-01
 *
 * ## Import
 *
 * App Service Source GitHub Tokens can be imported using the `resource id`, e.g.
 *
 * ```sh
 * $ pulumi import azure:appservice/sourceControlToken:SourceControlToken example /providers/Microsoft.Web/sourceControls/GitHub
 * ```
 */
export class SourceControlToken extends pulumi.CustomResource {
    /**
     * Get an existing SourceControlToken resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SourceControlTokenState, opts?: pulumi.CustomResourceOptions): SourceControlToken {
        return new SourceControlToken(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'azure:appservice/sourceControlToken:SourceControlToken';

    /**
     * Returns true if the given object is an instance of SourceControlToken.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SourceControlToken {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SourceControlToken.__pulumiType;
    }

    /**
     * The Access Token.
     */
    declare public readonly token: pulumi.Output<string>;
    /**
     * The Access Token Secret.
     *
     * > **Note:** The token used for deploying App Service needs the following permissions: `repo` and `workflow`.
     */
    declare public readonly tokenSecret: pulumi.Output<string | undefined>;
    /**
     * The Token type. Possible values include `Bitbucket`, `Dropbox`, `Github`, and `OneDrive`.
     */
    declare public readonly type: pulumi.Output<string>;

    /**
     * Create a SourceControlToken resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SourceControlTokenArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SourceControlTokenArgs | SourceControlTokenState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SourceControlTokenState | undefined;
            resourceInputs["token"] = state?.token;
            resourceInputs["tokenSecret"] = state?.tokenSecret;
            resourceInputs["type"] = state?.type;
        } else {
            const args = argsOrState as SourceControlTokenArgs | undefined;
            if (args?.token === undefined && !opts.urn) {
                throw new Error("Missing required property 'token'");
            }
            if (args?.type === undefined && !opts.urn) {
                throw new Error("Missing required property 'type'");
            }
            resourceInputs["token"] = args?.token ? pulumi.secret(args.token) : undefined;
            resourceInputs["tokenSecret"] = args?.tokenSecret ? pulumi.secret(args.tokenSecret) : undefined;
            resourceInputs["type"] = args?.type;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["token", "tokenSecret"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(SourceControlToken.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SourceControlToken resources.
 */
export interface SourceControlTokenState {
    /**
     * The Access Token.
     */
    token?: pulumi.Input<string>;
    /**
     * The Access Token Secret.
     *
     * > **Note:** The token used for deploying App Service needs the following permissions: `repo` and `workflow`.
     */
    tokenSecret?: pulumi.Input<string>;
    /**
     * The Token type. Possible values include `Bitbucket`, `Dropbox`, `Github`, and `OneDrive`.
     */
    type?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a SourceControlToken resource.
 */
export interface SourceControlTokenArgs {
    /**
     * The Access Token.
     */
    token: pulumi.Input<string>;
    /**
     * The Access Token Secret.
     *
     * > **Note:** The token used for deploying App Service needs the following permissions: `repo` and `workflow`.
     */
    tokenSecret?: pulumi.Input<string>;
    /**
     * The Token type. Possible values include `Bitbucket`, `Dropbox`, `Github`, and `OneDrive`.
     */
    type: pulumi.Input<string>;
}
