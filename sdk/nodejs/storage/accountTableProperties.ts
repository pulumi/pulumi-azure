// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Manages the Table Properties of an Azure Storage Account.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azure from "@pulumi/azure";
 *
 * const example = new azure.core.ResourceGroup("example", {
 *     name: "example-resources",
 *     location: "West Europe",
 * });
 * const exampleAccount = new azure.storage.Account("example", {
 *     name: "storageaccountname",
 *     resourceGroupName: example.name,
 *     location: example.location,
 *     accountTier: "Standard",
 *     accountReplicationType: "GRS",
 *     tags: {
 *         environment: "staging",
 *     },
 * });
 * const exampleAccountTableProperties = new azure.storage.AccountTableProperties("example", {
 *     storageAccountId: exampleAccount.id,
 *     corsRules: [{
 *         allowedOrigins: ["http://www.example.com"],
 *         exposedHeaders: ["x-tempo-*"],
 *         allowedHeaders: ["x-tempo-*"],
 *         allowedMethods: [
 *             "GET",
 *             "PUT",
 *         ],
 *         maxAgeInSeconds: 500,
 *     }],
 *     logging: {
 *         version: "1.0",
 *         "delete": true,
 *         read: true,
 *         write: true,
 *         retentionPolicyDays: 7,
 *     },
 *     hourMetrics: {
 *         version: "1.0",
 *         retentionPolicyDays: 7,
 *     },
 *     minuteMetrics: {
 *         version: "1.0",
 *         retentionPolicyDays: 7,
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Storage Account Table Properties can be imported using the `resource id`, e.g.
 *
 * ```sh
 * $ pulumi import azure:storage/accountTableProperties:AccountTableProperties example /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/myresourcegroup/providers/Microsoft.Storage/storageAccounts/myaccount
 * ```
 */
export class AccountTableProperties extends pulumi.CustomResource {
    /**
     * Get an existing AccountTableProperties resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AccountTablePropertiesState, opts?: pulumi.CustomResourceOptions): AccountTableProperties {
        return new AccountTableProperties(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'azure:storage/accountTableProperties:AccountTableProperties';

    /**
     * Returns true if the given object is an instance of AccountTableProperties.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AccountTableProperties {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AccountTableProperties.__pulumiType;
    }

    /**
     * A `corsRule` block as defined below.
     */
    declare public readonly corsRules: pulumi.Output<outputs.storage.AccountTablePropertiesCorsRule[] | undefined>;
    /**
     * A `hourMetrics` block as defined below.
     *
     * > **Note:** At least one of `corsRule`, `logging`, `minuteMetrics`, or `hourMetrics` must be specified.
     */
    declare public readonly hourMetrics: pulumi.Output<outputs.storage.AccountTablePropertiesHourMetrics>;
    /**
     * A `logging` block as defined below.
     */
    declare public readonly logging: pulumi.Output<outputs.storage.AccountTablePropertiesLogging>;
    /**
     * A `minuteMetrics` block as defined below.
     */
    declare public readonly minuteMetrics: pulumi.Output<outputs.storage.AccountTablePropertiesMinuteMetrics>;
    /**
     * The ID of the Storage Account to set Table Properties on. Changing this forces a new resource to be created.
     */
    declare public readonly storageAccountId: pulumi.Output<string>;

    /**
     * Create a AccountTableProperties resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AccountTablePropertiesArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AccountTablePropertiesArgs | AccountTablePropertiesState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AccountTablePropertiesState | undefined;
            resourceInputs["corsRules"] = state?.corsRules;
            resourceInputs["hourMetrics"] = state?.hourMetrics;
            resourceInputs["logging"] = state?.logging;
            resourceInputs["minuteMetrics"] = state?.minuteMetrics;
            resourceInputs["storageAccountId"] = state?.storageAccountId;
        } else {
            const args = argsOrState as AccountTablePropertiesArgs | undefined;
            if (args?.storageAccountId === undefined && !opts.urn) {
                throw new Error("Missing required property 'storageAccountId'");
            }
            resourceInputs["corsRules"] = args?.corsRules;
            resourceInputs["hourMetrics"] = args?.hourMetrics;
            resourceInputs["logging"] = args?.logging;
            resourceInputs["minuteMetrics"] = args?.minuteMetrics;
            resourceInputs["storageAccountId"] = args?.storageAccountId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AccountTableProperties.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AccountTableProperties resources.
 */
export interface AccountTablePropertiesState {
    /**
     * A `corsRule` block as defined below.
     */
    corsRules?: pulumi.Input<pulumi.Input<inputs.storage.AccountTablePropertiesCorsRule>[] | undefined>;
    /**
     * A `hourMetrics` block as defined below.
     *
     * > **Note:** At least one of `corsRule`, `logging`, `minuteMetrics`, or `hourMetrics` must be specified.
     */
    hourMetrics?: pulumi.Input<inputs.storage.AccountTablePropertiesHourMetrics | undefined>;
    /**
     * A `logging` block as defined below.
     */
    logging?: pulumi.Input<inputs.storage.AccountTablePropertiesLogging | undefined>;
    /**
     * A `minuteMetrics` block as defined below.
     */
    minuteMetrics?: pulumi.Input<inputs.storage.AccountTablePropertiesMinuteMetrics | undefined>;
    /**
     * The ID of the Storage Account to set Table Properties on. Changing this forces a new resource to be created.
     */
    storageAccountId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a AccountTableProperties resource.
 */
export interface AccountTablePropertiesArgs {
    /**
     * A `corsRule` block as defined below.
     */
    corsRules?: pulumi.Input<pulumi.Input<inputs.storage.AccountTablePropertiesCorsRule>[] | undefined>;
    /**
     * A `hourMetrics` block as defined below.
     *
     * > **Note:** At least one of `corsRule`, `logging`, `minuteMetrics`, or `hourMetrics` must be specified.
     */
    hourMetrics?: pulumi.Input<inputs.storage.AccountTablePropertiesHourMetrics | undefined>;
    /**
     * A `logging` block as defined below.
     */
    logging?: pulumi.Input<inputs.storage.AccountTablePropertiesLogging | undefined>;
    /**
     * A `minuteMetrics` block as defined below.
     */
    minuteMetrics?: pulumi.Input<inputs.storage.AccountTablePropertiesMinuteMetrics | undefined>;
    /**
     * The ID of the Storage Account to set Table Properties on. Changing this forces a new resource to be created.
     */
    storageAccountId: pulumi.Input<string>;
}
