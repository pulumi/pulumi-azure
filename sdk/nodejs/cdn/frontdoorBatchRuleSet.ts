// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Manages a Front Door (standard/premium) Batch Rule Set.
 *
 * > **Note:** This resource creates the Front Door Rule Set in batch mode and manages the full ordered batch rule collection for it. Any change to the configured `rule` blocks sends the desired final ordered rule list to the Resource Provider in a single request.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azure from "@pulumi/azure";
 *
 * const example = new azure.core.ResourceGroup("example", {
 *     name: "example-cdn-frontdoor",
 *     location: "West Europe",
 * });
 * const exampleFrontdoorProfile = new azure.cdn.FrontdoorProfile("example", {
 *     name: "example-profile",
 *     resourceGroupName: example.name,
 *     skuName: "Premium_AzureFrontDoor",
 * });
 * const exampleFrontdoorEndpoint = new azure.cdn.FrontdoorEndpoint("example", {
 *     name: "example-endpoint",
 *     cdnFrontdoorProfileId: exampleFrontdoorProfile.id,
 *     tags: {
 *         endpoint: "contoso.com",
 *     },
 * });
 * const exampleFrontdoorOriginGroup = new azure.cdn.FrontdoorOriginGroup("example", {
 *     name: "example-originGroup",
 *     cdnFrontdoorProfileId: exampleFrontdoorProfile.id,
 *     sessionAffinityEnabled: true,
 *     restoreTrafficTimeToHealedOrNewEndpointInMinutes: 10,
 *     healthProbe: {
 *         intervalInSeconds: 240,
 *         path: "/healthProbe",
 *         protocol: "Https",
 *         requestType: "GET",
 *     },
 *     loadBalancing: {
 *         additionalLatencyInMilliseconds: 0,
 *         sampleSize: 16,
 *         successfulSamplesRequired: 3,
 *     },
 * });
 * const exampleFrontdoorOrigin = new azure.cdn.FrontdoorOrigin("example", {
 *     name: "example-origin",
 *     cdnFrontdoorOriginGroupId: exampleFrontdoorOriginGroup.id,
 *     enabled: true,
 *     certificateNameCheckEnabled: false,
 *     hostName: exampleFrontdoorEndpoint.hostName,
 *     httpPort: 80,
 *     httpsPort: 443,
 *     originHostHeader: "contoso.com",
 *     priority: 1,
 *     weight: 500,
 * });
 * const exampleFrontdoorBatchRuleSet = new azure.cdn.FrontdoorBatchRuleSet("example", {
 *     name: "examplebatchruleset",
 *     cdnFrontdoorProfileId: exampleFrontdoorProfile.id,
 *     rules: [{
 *         name: "examplebatchrule",
 *         order: 1,
 *         behaviourOnMatch: "Continue",
 *         actions: {
 *             routeConfigurationOverride: {
 *                 originGroup: {
 *                     cdnFrontdoorOriginGroupId: exampleFrontdoorOriginGroup.id,
 *                     forwardingProtocol: "HttpsOnly",
 *                 },
 *                 caching: {
 *                     behaviour: "OverrideIfOriginMissing",
 *                     duration: "365.23:59:59",
 *                     compressionEnabled: true,
 *                     queryStringBehaviour: "IncludeSpecifiedQueryStrings",
 *                     queryStringParameters: [
 *                         "foo",
 *                         "clientIp={client_ip}",
 *                     ],
 *                 },
 *             },
 *         },
 *         conditions: {
 *             hostNames: [{
 *                 operator: "Equal",
 *                 values: [
 *                     "www.contoso.com",
 *                     "images.contoso.com",
 *                     "video.contoso.com",
 *                 ],
 *                 transforms: [
 *                     "Lowercase",
 *                     "Trim",
 *                 ],
 *             }],
 *             deviceTypes: [{
 *                 operator: "Equal",
 *                 values: "Mobile",
 *             }],
 *             postArguments: [{
 *                 name: "customerName",
 *                 operator: "BeginsWith",
 *                 values: [
 *                     "J",
 *                     "K",
 *                 ],
 *                 transforms: ["Uppercase"],
 *             }],
 *             requestMethods: [{
 *                 operator: "Equal",
 *                 values: ["DELETE"],
 *             }],
 *             requestFilenames: [{
 *                 operator: "Equal",
 *                 values: ["media.mp4"],
 *                 transforms: [
 *                     "Lowercase",
 *                     "RemoveNulls",
 *                     "Trim",
 *                 ],
 *             }],
 *         },
 *     }],
 * });
 * const exampleFrontdoorRoute = new azure.cdn.FrontdoorRoute("example", {
 *     name: "example-cdn-frontdoor-route",
 *     cdnFrontdoorEndpointId: exampleFrontdoorEndpoint.id,
 *     cdnFrontdoorOriginGroupId: exampleFrontdoorOriginGroup.id,
 *     cdnFrontdoorOriginIds: [exampleFrontdoorOrigin.id],
 *     cdnFrontdoorRuleSetIds: [exampleFrontdoorBatchRuleSet.id],
 *     patternsToMatches: ["/*"],
 *     supportedProtocols: [
 *         "Http",
 *         "Https",
 *     ],
 * });
 * ```
 *
 * ## API Providers
 *
 * <!-- This section is generated, changes will be overwritten -->
 * This resource uses the following Azure API Providers:
 *
 * * `Microsoft.Cdn` - 2025-12-01
 *
 * ## Import
 *
 * A Front Door Batch Rule Set can be imported using the `resource id`, e.g.
 *
 * ```sh
 * $ pulumi import azure:cdn/frontdoorBatchRuleSet:FrontdoorBatchRuleSet example /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/resourceGroup1/providers/Microsoft.Cdn/profiles/profile1/ruleSets/ruleSet1
 * ```
 *
 * > **Note:** Only Rule Sets that were provisioned in batch mode can be managed by this resource. Importing a Rule Set that was not provisioned in batch mode returns an error - use `azure.cdn.FrontdoorRuleSet` instead.
 */
export class FrontdoorBatchRuleSet extends pulumi.CustomResource {
    /**
     * Get an existing FrontdoorBatchRuleSet resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FrontdoorBatchRuleSetState, opts?: pulumi.CustomResourceOptions): FrontdoorBatchRuleSet {
        return new FrontdoorBatchRuleSet(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'azure:cdn/frontdoorBatchRuleSet:FrontdoorBatchRuleSet';

    /**
     * Returns true if the given object is an instance of FrontdoorBatchRuleSet.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FrontdoorBatchRuleSet {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FrontdoorBatchRuleSet.__pulumiType;
    }

    /**
     * The resource ID of the Front Door Profile where this Front Door Batch Rule Set should be created. Changing this forces a new resource to be created.
     */
    declare public readonly cdnFrontdoorProfileId: pulumi.Output<string>;
    /**
     * The name which should be used for this Front Door Batch Rule Set. Changing this forces a new resource to be created.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block's `order` value. A maximum of `100` `rule` blocks may be defined.
     *
     * > **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
     *
     * > **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
     *
     * > **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
     */
    declare public readonly rules: pulumi.Output<outputs.cdn.FrontdoorBatchRuleSetRule[]>;

    /**
     * Create a FrontdoorBatchRuleSet resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FrontdoorBatchRuleSetArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FrontdoorBatchRuleSetArgs | FrontdoorBatchRuleSetState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FrontdoorBatchRuleSetState | undefined;
            resourceInputs["cdnFrontdoorProfileId"] = state?.cdnFrontdoorProfileId;
            resourceInputs["name"] = state?.name;
            resourceInputs["rules"] = state?.rules;
        } else {
            const args = argsOrState as FrontdoorBatchRuleSetArgs | undefined;
            if (args?.cdnFrontdoorProfileId === undefined && !opts.urn) {
                throw new Error("Missing required property 'cdnFrontdoorProfileId'");
            }
            if (args?.rules === undefined && !opts.urn) {
                throw new Error("Missing required property 'rules'");
            }
            resourceInputs["cdnFrontdoorProfileId"] = args?.cdnFrontdoorProfileId;
            resourceInputs["name"] = args?.name;
            resourceInputs["rules"] = args?.rules;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FrontdoorBatchRuleSet.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FrontdoorBatchRuleSet resources.
 */
export interface FrontdoorBatchRuleSetState {
    /**
     * The resource ID of the Front Door Profile where this Front Door Batch Rule Set should be created. Changing this forces a new resource to be created.
     */
    cdnFrontdoorProfileId?: pulumi.Input<string | undefined>;
    /**
     * The name which should be used for this Front Door Batch Rule Set. Changing this forces a new resource to be created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block's `order` value. A maximum of `100` `rule` blocks may be defined.
     *
     * > **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
     *
     * > **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
     *
     * > **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.cdn.FrontdoorBatchRuleSetRule>[] | undefined>;
}

/**
 * The set of arguments for constructing a FrontdoorBatchRuleSet resource.
 */
export interface FrontdoorBatchRuleSetArgs {
    /**
     * The resource ID of the Front Door Profile where this Front Door Batch Rule Set should be created. Changing this forces a new resource to be created.
     */
    cdnFrontdoorProfileId: pulumi.Input<string>;
    /**
     * The name which should be used for this Front Door Batch Rule Set. Changing this forces a new resource to be created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * One or more `rule` blocks as defined below. The configured blocks represent the complete set of rules managed for this Front Door Batch Rule Set. The final rule ordering is determined by each block's `order` value. A maximum of `100` `rule` blocks may be defined.
     *
     * > **Note:** The `rule` blocks must be declared in ascending `order`, gaps between different rules are allowed. To insert, remove, or move a rule, update the full `rule` collection in the same ascending order that you want Terraform to store.
     *
     * > **Note:** Each `rule` block must use a unique `name` value and a unique `order` value.
     *
     * > **Note:** Each `rule` that enables caching (using the `route_configuration_override.caching` block with a `behaviour` other than `Disabled`) consumes two of the `100` available rule slots. The plan fails if the effective number of rule slots exceeds this service-side quota.
     */
    rules: pulumi.Input<pulumi.Input<inputs.cdn.FrontdoorBatchRuleSetRule>[]>;
}
