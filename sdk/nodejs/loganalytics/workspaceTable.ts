// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages a Table in a Log Analytics (formally Operational Insights) Workspace.
 *
 * > **Note:** This resource does not create or destroy tables. This resource is used to update attributes (currently only retention_in_days) of the tables created when a Log Analytics Workspace is created. Deleting an azure.loganalytics.WorkspaceTable resource will not delete the table. Instead, the table's retentionInDays field will be set to the value of azure.operationalinsights.AnalyticsWorkspace retention_in_days
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azure from "@pulumi/azure";
 *
 * const example = new azure.core.ResourceGroup("example", {
 *     name: "example-resources",
 *     location: "West Europe",
 * });
 * const exampleAnalyticsWorkspace = new azure.operationalinsights.AnalyticsWorkspace("example", {
 *     name: "example",
 *     location: example.location,
 *     resourceGroupName: example.name,
 *     sku: "PerGB2018",
 *     retentionInDays: 30,
 * });
 * const exampleWorkspaceTable = new azure.loganalytics.WorkspaceTable("example", {
 *     workspaceId: exampleAnalyticsWorkspace.id,
 *     name: "AppMetrics",
 *     retentionInDays: 60,
 *     totalRetentionInDays: 180,
 * });
 * ```
 *
 * ## API Providers
 *
 * <!-- This section is generated, changes will be overwritten -->
 * This resource uses the following Azure API Providers:
 *
 * * `Microsoft.OperationalInsights` - 2022-10-01
 */
export class WorkspaceTable extends pulumi.CustomResource {
    /**
     * Get an existing WorkspaceTable resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: WorkspaceTableState, opts?: pulumi.CustomResourceOptions): WorkspaceTable {
        return new WorkspaceTable(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'azure:loganalytics/workspaceTable:WorkspaceTable';

    /**
     * Returns true if the given object is an instance of WorkspaceTable.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is WorkspaceTable {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === WorkspaceTable.__pulumiType;
    }

    /**
     * Specifies the name of a table in a Log Analytics Workspace.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Specify the system how to handle and charge the logs ingested to the table. Possible values are `Analytics` and `Basic`. Defaults to `Analytics`.
     *
     * > **Note:** The `name` of tables currently supported by the `Basic` plan can be found [here](https://learn.microsoft.com/en-us/azure/azure-monitor/logs/basic-logs-azure-tables).
     */
    declare public readonly plan: pulumi.Output<string | undefined>;
    /**
     * The table's retention in days. Possible values are either `8` (Basic Tier only) or range between `4` and `730`.
     */
    declare public readonly retentionInDays: pulumi.Output<number | undefined>;
    /**
     * The table's total retention in days. Possible values range between `4` and `730`; or `1095`, `1460`, `1826`, `2191`, `2556`, `2922`, `3288`, `3653`, `4018`, or `4383`.
     *
     * > **Note:** `retentionInDays` and `totalRetentionInDays` will revert back to the value of azure.operationalinsights.AnalyticsWorkspace retention_in_days when a azure.loganalytics.WorkspaceTable is deleted.
     *
     * > **Note:** The `retentionInDays` cannot be specified when `plan` is `Basic` because the retention is fixed at eight days.
     */
    declare public readonly totalRetentionInDays: pulumi.Output<number | undefined>;
    /**
     * The object ID of the Log Analytics Workspace that contains the table.
     */
    declare public readonly workspaceId: pulumi.Output<string>;

    /**
     * Create a WorkspaceTable resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: WorkspaceTableArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: WorkspaceTableArgs | WorkspaceTableState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as WorkspaceTableState | undefined;
            resourceInputs["name"] = state?.name;
            resourceInputs["plan"] = state?.plan;
            resourceInputs["retentionInDays"] = state?.retentionInDays;
            resourceInputs["totalRetentionInDays"] = state?.totalRetentionInDays;
            resourceInputs["workspaceId"] = state?.workspaceId;
        } else {
            const args = argsOrState as WorkspaceTableArgs | undefined;
            if (args?.workspaceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'workspaceId'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["plan"] = args?.plan;
            resourceInputs["retentionInDays"] = args?.retentionInDays;
            resourceInputs["totalRetentionInDays"] = args?.totalRetentionInDays;
            resourceInputs["workspaceId"] = args?.workspaceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(WorkspaceTable.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering WorkspaceTable resources.
 */
export interface WorkspaceTableState {
    /**
     * Specifies the name of a table in a Log Analytics Workspace.
     */
    name?: pulumi.Input<string>;
    /**
     * Specify the system how to handle and charge the logs ingested to the table. Possible values are `Analytics` and `Basic`. Defaults to `Analytics`.
     *
     * > **Note:** The `name` of tables currently supported by the `Basic` plan can be found [here](https://learn.microsoft.com/en-us/azure/azure-monitor/logs/basic-logs-azure-tables).
     */
    plan?: pulumi.Input<string>;
    /**
     * The table's retention in days. Possible values are either `8` (Basic Tier only) or range between `4` and `730`.
     */
    retentionInDays?: pulumi.Input<number>;
    /**
     * The table's total retention in days. Possible values range between `4` and `730`; or `1095`, `1460`, `1826`, `2191`, `2556`, `2922`, `3288`, `3653`, `4018`, or `4383`.
     *
     * > **Note:** `retentionInDays` and `totalRetentionInDays` will revert back to the value of azure.operationalinsights.AnalyticsWorkspace retention_in_days when a azure.loganalytics.WorkspaceTable is deleted.
     *
     * > **Note:** The `retentionInDays` cannot be specified when `plan` is `Basic` because the retention is fixed at eight days.
     */
    totalRetentionInDays?: pulumi.Input<number>;
    /**
     * The object ID of the Log Analytics Workspace that contains the table.
     */
    workspaceId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a WorkspaceTable resource.
 */
export interface WorkspaceTableArgs {
    /**
     * Specifies the name of a table in a Log Analytics Workspace.
     */
    name?: pulumi.Input<string>;
    /**
     * Specify the system how to handle and charge the logs ingested to the table. Possible values are `Analytics` and `Basic`. Defaults to `Analytics`.
     *
     * > **Note:** The `name` of tables currently supported by the `Basic` plan can be found [here](https://learn.microsoft.com/en-us/azure/azure-monitor/logs/basic-logs-azure-tables).
     */
    plan?: pulumi.Input<string>;
    /**
     * The table's retention in days. Possible values are either `8` (Basic Tier only) or range between `4` and `730`.
     */
    retentionInDays?: pulumi.Input<number>;
    /**
     * The table's total retention in days. Possible values range between `4` and `730`; or `1095`, `1460`, `1826`, `2191`, `2556`, `2922`, `3288`, `3653`, `4018`, or `4383`.
     *
     * > **Note:** `retentionInDays` and `totalRetentionInDays` will revert back to the value of azure.operationalinsights.AnalyticsWorkspace retention_in_days when a azure.loganalytics.WorkspaceTable is deleted.
     *
     * > **Note:** The `retentionInDays` cannot be specified when `plan` is `Basic` because the retention is fixed at eight days.
     */
    totalRetentionInDays?: pulumi.Input<number>;
    /**
     * The object ID of the Log Analytics Workspace that contains the table.
     */
    workspaceId: pulumi.Input<string>;
}
