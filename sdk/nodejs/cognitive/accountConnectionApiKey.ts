// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages a Cognitive Services (Microsoft Foundry) Account Connection with API Key authentication.
 *
 * > **Note:** In the new Foundry portal experience, "Account Connections" are shown as "Tools" under the "Build" menu.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azure from "@pulumi/azure";
 *
 * const example = new azure.core.ResourceGroup("example", {
 *     name: "example-resources",
 *     location: "West Europe",
 * });
 * const exampleAccount = new azure.cognitive.Account("example", {
 *     name: "example-aiservices",
 *     location: example.location,
 *     resourceGroupName: example.name,
 *     kind: "AIServices",
 *     skuName: "S0",
 *     projectManagementEnabled: true,
 *     customSubdomainName: "exampleaiservices",
 *     identity: {
 *         type: "SystemAssigned",
 *     },
 * });
 * const openai = new azure.cognitive.Account("openai", {
 *     name: "example-openai",
 *     location: example.location,
 *     resourceGroupName: example.name,
 *     kind: "OpenAI",
 *     skuName: "S0",
 *     identity: {
 *         type: "SystemAssigned",
 *     },
 * });
 * const exampleAccountConnectionApiKey = new azure.cognitive.AccountConnectionApiKey("example", {
 *     name: "example-connection",
 *     cognitiveAccountId: exampleAccount.id,
 *     category: "AzureOpenAI",
 *     target: openai.endpoint,
 *     apiKey: openai.primaryAccessKey,
 *     metadata: {
 *         ApiType: "Azure",
 *         ResourceId: openai.id,
 *         Location: openai.location,
 *     },
 * });
 * ```
 *
 * ## API Providers
 *
 * <!-- This section is generated, changes will be overwritten -->
 * This resource uses the following Azure API Providers:
 *
 * * `Microsoft.CognitiveServices` - 2026-03-01
 *
 * ## Import
 *
 * A Cognitive Services (Microsoft Foundry) Account Connection with API Key authentication can be imported using the `resource id`, e.g.
 *
 * ```sh
 * $ pulumi import azure:cognitive/accountConnectionApiKey:AccountConnectionApiKey example /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/resourceGroup1/providers/Microsoft.CognitiveServices/accounts/account1/connections/connection1
 * ```
 */
export class AccountConnectionApiKey extends pulumi.CustomResource {
    /**
     * Get an existing AccountConnectionApiKey resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AccountConnectionApiKeyState, opts?: pulumi.CustomResourceOptions): AccountConnectionApiKey {
        return new AccountConnectionApiKey(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'azure:cognitive/accountConnectionApiKey:AccountConnectionApiKey';

    /**
     * Returns true if the given object is an instance of AccountConnectionApiKey.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AccountConnectionApiKey {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AccountConnectionApiKey.__pulumiType;
    }

    /**
     * The API key for authentication. This field is sensitive.
     */
    declare public readonly apiKey: pulumi.Output<string>;
    /**
     * The category of the connection. Possible values are `AIServices`, `ApiKey`, `ApiManagement`, `AppConfig`, `AppInsights`, `AzureOpenAI`, `BingLLMSearch`, `CognitiveSearch`, `CognitiveService`, `GroundingWithBingSearch`, `GroundingWithCustomSearch`, `ModelGateway`, `OpenAI`, `Pinecone`, `Serp`, and `Serverless`. Changing this forces a new resource to be created.
     */
    declare public readonly category: pulumi.Output<string>;
    /**
     * The ID of the Cognitive Services Account. Changing this forces a new resource to be created.
     */
    declare public readonly cognitiveAccountId: pulumi.Output<string>;
    /**
     * A mapping of metadata key-value pairs for the connection.
     *
     * > **Note:** The `metadata` map must include `ResourceId` when `category` is `AIServices`, `ApiType` when `category` is `AzureOpenAI`, `Location` when `category` is `BingLLMSearch`, and `Kind` when `category` is `CognitiveService`. To check any additional metadata returned by Azure, create an equivalent connection in the Foundry portal and inspect it with `az rest --method get --url "{connection_resource_id}?api-version=2026-03-01"`.
     */
    declare public readonly metadata: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * The name of the Cognitive Services Account Connection. Changing this forces a new resource to be created.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The target endpoint or resource for the connection.
     *
     * > **Note:** `target` must be specified unless `category` is set to `OpenAI`, `Pinecone`, or `Serp`.
     */
    declare public readonly target: pulumi.Output<string | undefined>;

    /**
     * Create a AccountConnectionApiKey resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AccountConnectionApiKeyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AccountConnectionApiKeyArgs | AccountConnectionApiKeyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AccountConnectionApiKeyState | undefined;
            resourceInputs["apiKey"] = state?.apiKey;
            resourceInputs["category"] = state?.category;
            resourceInputs["cognitiveAccountId"] = state?.cognitiveAccountId;
            resourceInputs["metadata"] = state?.metadata;
            resourceInputs["name"] = state?.name;
            resourceInputs["target"] = state?.target;
        } else {
            const args = argsOrState as AccountConnectionApiKeyArgs | undefined;
            if (args?.apiKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'apiKey'");
            }
            if (args?.category === undefined && !opts.urn) {
                throw new Error("Missing required property 'category'");
            }
            if (args?.cognitiveAccountId === undefined && !opts.urn) {
                throw new Error("Missing required property 'cognitiveAccountId'");
            }
            resourceInputs["apiKey"] = args?.apiKey ? pulumi.secret(args.apiKey) : undefined;
            resourceInputs["category"] = args?.category;
            resourceInputs["cognitiveAccountId"] = args?.cognitiveAccountId;
            resourceInputs["metadata"] = args?.metadata;
            resourceInputs["name"] = args?.name;
            resourceInputs["target"] = args?.target;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["apiKey"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(AccountConnectionApiKey.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AccountConnectionApiKey resources.
 */
export interface AccountConnectionApiKeyState {
    /**
     * The API key for authentication. This field is sensitive.
     */
    apiKey?: pulumi.Input<string | undefined>;
    /**
     * The category of the connection. Possible values are `AIServices`, `ApiKey`, `ApiManagement`, `AppConfig`, `AppInsights`, `AzureOpenAI`, `BingLLMSearch`, `CognitiveSearch`, `CognitiveService`, `GroundingWithBingSearch`, `GroundingWithCustomSearch`, `ModelGateway`, `OpenAI`, `Pinecone`, `Serp`, and `Serverless`. Changing this forces a new resource to be created.
     */
    category?: pulumi.Input<string | undefined>;
    /**
     * The ID of the Cognitive Services Account. Changing this forces a new resource to be created.
     */
    cognitiveAccountId?: pulumi.Input<string | undefined>;
    /**
     * A mapping of metadata key-value pairs for the connection.
     *
     * > **Note:** The `metadata` map must include `ResourceId` when `category` is `AIServices`, `ApiType` when `category` is `AzureOpenAI`, `Location` when `category` is `BingLLMSearch`, and `Kind` when `category` is `CognitiveService`. To check any additional metadata returned by Azure, create an equivalent connection in the Foundry portal and inspect it with `az rest --method get --url "{connection_resource_id}?api-version=2026-03-01"`.
     */
    metadata?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * The name of the Cognitive Services Account Connection. Changing this forces a new resource to be created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The target endpoint or resource for the connection.
     *
     * > **Note:** `target` must be specified unless `category` is set to `OpenAI`, `Pinecone`, or `Serp`.
     */
    target?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a AccountConnectionApiKey resource.
 */
export interface AccountConnectionApiKeyArgs {
    /**
     * The API key for authentication. This field is sensitive.
     */
    apiKey: pulumi.Input<string>;
    /**
     * The category of the connection. Possible values are `AIServices`, `ApiKey`, `ApiManagement`, `AppConfig`, `AppInsights`, `AzureOpenAI`, `BingLLMSearch`, `CognitiveSearch`, `CognitiveService`, `GroundingWithBingSearch`, `GroundingWithCustomSearch`, `ModelGateway`, `OpenAI`, `Pinecone`, `Serp`, and `Serverless`. Changing this forces a new resource to be created.
     */
    category: pulumi.Input<string>;
    /**
     * The ID of the Cognitive Services Account. Changing this forces a new resource to be created.
     */
    cognitiveAccountId: pulumi.Input<string>;
    /**
     * A mapping of metadata key-value pairs for the connection.
     *
     * > **Note:** The `metadata` map must include `ResourceId` when `category` is `AIServices`, `ApiType` when `category` is `AzureOpenAI`, `Location` when `category` is `BingLLMSearch`, and `Kind` when `category` is `CognitiveService`. To check any additional metadata returned by Azure, create an equivalent connection in the Foundry portal and inspect it with `az rest --method get --url "{connection_resource_id}?api-version=2026-03-01"`.
     */
    metadata?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * The name of the Cognitive Services Account Connection. Changing this forces a new resource to be created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The target endpoint or resource for the connection.
     *
     * > **Note:** `target` must be specified unless `category` is set to `OpenAI`, `Pinecone`, or `Serp`.
     */
    target?: pulumi.Input<string | undefined>;
}
