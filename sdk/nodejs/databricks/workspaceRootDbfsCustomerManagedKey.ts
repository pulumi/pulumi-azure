// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages a Customer Managed Key for the Databricks Workspaces Root Databricks File System (DBFS)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azure from "@pulumi/azure";
 *
 * const current = azure.core.getClientConfig({});
 * const example = new azure.core.ResourceGroup("example", {
 *     name: "example-resources",
 *     location: "West Europe",
 * });
 * const exampleKeyVault = new azure.keyvault.KeyVault("example", {
 *     name: "examplekeyvault",
 *     location: example.location,
 *     resourceGroupName: example.name,
 *     tenantId: current.then(current => current.tenantId),
 *     skuName: "premium",
 *     purgeProtectionEnabled: true,
 *     softDeleteRetentionDays: 7,
 * });
 * const terraform = new azure.keyvault.AccessPolicy("terraform", {
 *     keyVaultId: exampleKeyVault.id,
 *     tenantId: exampleKeyVault.tenantId,
 *     objectId: current.then(current => current.objectId),
 *     keyPermissions: [
 *         "Create",
 *         "Delete",
 *         "Get",
 *         "Purge",
 *         "Recover",
 *         "Update",
 *         "List",
 *         "Decrypt",
 *         "Sign",
 *         "GetRotationPolicy",
 *     ],
 * });
 * const exampleKey = new azure.keyvault.Key("example", {
 *     name: "example-certificate",
 *     keyVaultId: exampleKeyVault.id,
 *     keyType: "RSA",
 *     keySize: 2048,
 *     keyOpts: [
 *         "decrypt",
 *         "encrypt",
 *         "sign",
 *         "unwrapKey",
 *         "verify",
 *         "wrapKey",
 *     ],
 * }, {
 *     dependsOn: [terraform],
 * });
 * const exampleWorkspace = new azure.databricks.Workspace("example", {
 *     name: "example-workspace",
 *     resourceGroupName: example.name,
 *     location: example.location,
 *     sku: "premium",
 *     customerManagedKeyEnabled: true,
 * });
 * const databricks = new azure.keyvault.AccessPolicy("databricks", {
 *     keyVaultId: exampleKeyVault.id,
 *     tenantId: exampleWorkspace.storageAccountIdentities.apply(storageAccountIdentities => storageAccountIdentities[0].tenantId),
 *     objectId: exampleWorkspace.storageAccountIdentities.apply(storageAccountIdentities => storageAccountIdentities[0].principalId),
 *     keyPermissions: [
 *         "Create",
 *         "Delete",
 *         "Get",
 *         "Purge",
 *         "Recover",
 *         "Update",
 *         "List",
 *         "Decrypt",
 *         "Sign",
 *     ],
 * }, {
 *     dependsOn: [exampleWorkspace],
 * });
 * const exampleWorkspaceRootDbfsCustomerManagedKey = new azure.databricks.WorkspaceRootDbfsCustomerManagedKey("example", {
 *     workspaceId: exampleWorkspace.id,
 *     keyVaultKeyId: exampleKey.id,
 * }, {
 *     dependsOn: [databricks],
 * });
 * ```
 *
 * ## Example HCL Configurations
 *
 * * Databricks Workspace with Root Databricks File System Customer Managed Keys
 * * Databricks Workspace with Root Databricks File System Customer Managed Keys in a Different Subscription
 * * Databricks Workspace with Private Endpoint, Customer Managed Keys for Managed Services and Root Databricks File System Customer Managed Keys
 *
 * ## API Providers
 *
 * <!-- This section is generated, changes will be overwritten -->
 * This resource uses the following Azure API Providers:
 *
 * * `Microsoft.Databricks` - 2024-05-01
 *
 * ## Import
 *
 * Databricks Workspace Root DBFS Customer Managed Key can be imported using the `resource id`, e.g.
 *
 * ```sh
 * $ pulumi import azure:databricks/workspaceRootDbfsCustomerManagedKey:WorkspaceRootDbfsCustomerManagedKey workspace1 /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/group1/providers/Microsoft.Databricks/workspaces/workspace1
 * ```
 */
export class WorkspaceRootDbfsCustomerManagedKey extends pulumi.CustomResource {
    /**
     * Get an existing WorkspaceRootDbfsCustomerManagedKey resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: WorkspaceRootDbfsCustomerManagedKeyState, opts?: pulumi.CustomResourceOptions): WorkspaceRootDbfsCustomerManagedKey {
        return new WorkspaceRootDbfsCustomerManagedKey(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'azure:databricks/workspaceRootDbfsCustomerManagedKey:WorkspaceRootDbfsCustomerManagedKey';

    /**
     * Returns true if the given object is an instance of WorkspaceRootDbfsCustomerManagedKey.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is WorkspaceRootDbfsCustomerManagedKey {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === WorkspaceRootDbfsCustomerManagedKey.__pulumiType;
    }

    /**
     * Specifies the Resource ID of the Key Vault which contains the `keyVaultKeyId`.
     *
     * > **Note:** The `keyVaultId` field only needs to be specified if the Key Vault which contains the `keyVaultKeyId` exists in a different subscription than the Databricks Workspace. If the `keyVaultId` field is not specified it is assumed that the `keyVaultKeyId` is hosted in the same subscription as the Databricks Workspace. Does not apply to managed HSM vaults.
     *
     * > **Note:** If you are using multiple service principals to execute Terraform across subscriptions you will need to add an additional `azure.keyvault.AccessPolicy` resource granting the service principal access to the key vault in that subscription.
     */
    declare public readonly keyVaultId: pulumi.Output<string | undefined>;
    /**
     * The ID of the Key Vault Key to be used.
     */
    declare public readonly keyVaultKeyId: pulumi.Output<string>;
    /**
     * The Resource ID of the Databricks Workspace.
     */
    declare public readonly workspaceId: pulumi.Output<string>;

    /**
     * Create a WorkspaceRootDbfsCustomerManagedKey resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: WorkspaceRootDbfsCustomerManagedKeyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: WorkspaceRootDbfsCustomerManagedKeyArgs | WorkspaceRootDbfsCustomerManagedKeyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as WorkspaceRootDbfsCustomerManagedKeyState | undefined;
            resourceInputs["keyVaultId"] = state?.keyVaultId;
            resourceInputs["keyVaultKeyId"] = state?.keyVaultKeyId;
            resourceInputs["workspaceId"] = state?.workspaceId;
        } else {
            const args = argsOrState as WorkspaceRootDbfsCustomerManagedKeyArgs | undefined;
            if (args?.keyVaultKeyId === undefined && !opts.urn) {
                throw new Error("Missing required property 'keyVaultKeyId'");
            }
            if (args?.workspaceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'workspaceId'");
            }
            resourceInputs["keyVaultId"] = args?.keyVaultId;
            resourceInputs["keyVaultKeyId"] = args?.keyVaultKeyId;
            resourceInputs["workspaceId"] = args?.workspaceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const aliasOpts = { aliases: [{ type: "azure:databricks/workspaceCustomerManagedKey:WorkspaceCustomerManagedKey" }] };
        opts = pulumi.mergeOptions(opts, aliasOpts);
        super(WorkspaceRootDbfsCustomerManagedKey.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering WorkspaceRootDbfsCustomerManagedKey resources.
 */
export interface WorkspaceRootDbfsCustomerManagedKeyState {
    /**
     * Specifies the Resource ID of the Key Vault which contains the `keyVaultKeyId`.
     *
     * > **Note:** The `keyVaultId` field only needs to be specified if the Key Vault which contains the `keyVaultKeyId` exists in a different subscription than the Databricks Workspace. If the `keyVaultId` field is not specified it is assumed that the `keyVaultKeyId` is hosted in the same subscription as the Databricks Workspace. Does not apply to managed HSM vaults.
     *
     * > **Note:** If you are using multiple service principals to execute Terraform across subscriptions you will need to add an additional `azure.keyvault.AccessPolicy` resource granting the service principal access to the key vault in that subscription.
     */
    keyVaultId?: pulumi.Input<string>;
    /**
     * The ID of the Key Vault Key to be used.
     */
    keyVaultKeyId?: pulumi.Input<string>;
    /**
     * The Resource ID of the Databricks Workspace.
     */
    workspaceId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a WorkspaceRootDbfsCustomerManagedKey resource.
 */
export interface WorkspaceRootDbfsCustomerManagedKeyArgs {
    /**
     * Specifies the Resource ID of the Key Vault which contains the `keyVaultKeyId`.
     *
     * > **Note:** The `keyVaultId` field only needs to be specified if the Key Vault which contains the `keyVaultKeyId` exists in a different subscription than the Databricks Workspace. If the `keyVaultId` field is not specified it is assumed that the `keyVaultKeyId` is hosted in the same subscription as the Databricks Workspace. Does not apply to managed HSM vaults.
     *
     * > **Note:** If you are using multiple service principals to execute Terraform across subscriptions you will need to add an additional `azure.keyvault.AccessPolicy` resource granting the service principal access to the key vault in that subscription.
     */
    keyVaultId?: pulumi.Input<string>;
    /**
     * The ID of the Key Vault Key to be used.
     */
    keyVaultKeyId: pulumi.Input<string>;
    /**
     * The Resource ID of the Databricks Workspace.
     */
    workspaceId: pulumi.Input<string>;
}
